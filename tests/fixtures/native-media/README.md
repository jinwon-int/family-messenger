`blue.mp4` is a generated 32x32 blue frame clip (0.2 seconds), no audio or human data.
Produced locally with FFmpeg lavfi color and libx264, pixel format yuv420p,
`-movflags +faststart`. API tests check byte/hash preservation, not browser playback.
No upstream messenger asset was copied. FFmpeg is not a runtime dependency.
